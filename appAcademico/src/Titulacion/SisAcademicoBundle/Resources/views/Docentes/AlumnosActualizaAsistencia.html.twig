<script src="{{ asset('js/sistemau.functions.js') }}"></script>
{#<link href="{{ asset('bower_components/bootstrap/dist/css/bootstrap.css')}}" type="text/css" rel="stylesheet"/>
#}<form id="form-actualizaAsis" name="form-actualizaAsis" method="post" action="{{ path('docentes_actualiza_asistencia') }}" onsubmit="return false;">     
<div class="row" id="tabla1">
                <div class="col-xs-12">
                  <div ng-controller="AppCtrl">
{#hh#}
</div>
                      
                        <!--   /.panel-heading 12-->
                        <div class="panel-body">
                          <div class="row" >
                            <div class="row " style="padding-left: 25px;">
                                    <div class="col-xs-3">
                                        <label class="control-label" >Docente :</label>
                                        <input class="form-control" id="disabledInput" type="text"  value="{{profesor}}" disabled />
                                    </div>
                                    <div class="col-xs-3">
                                        <label>Asignatura :</label>
                                        <input class="form-control" id="disabledInput" type="text" value="{{materia}}" disabled>
                                    </div>
                                    <div class="col-xs-3">
                                        <label>Curso :</label>
                                        <input class="form-control" id="disabledInput" type="text" value="{{paralelo}}" disabled>
                                    </div>
                            </div>
                            {#<div class="col-lg-3">
                                 <label class="control-label" >parcial :</label>
                                   <select id="combofecha" name="combofecha" onchange="mostrarAlumnos('notas','{{ path('docentes_actualizar_asistencia') }}','2345');" >   
                                          <option value="1"># 1</option>
                                            <option value="2"># 2</option>                       
                                    </select>
                            </div>#}
                                      
                            <div class="col-xs-3" style="padding-top: 25px;">
                                 <label class="control-label" >Fecha :</label>
                                   <select id="combofecha" name="combofecha">   
                                            {{ fecha | raw }}                       
                                    </select>
                            </div>
                               
                            
                            <div class="col-xs-12" style="margin-bottom: 20px;">
                                &nbsp;
                            </div>
                            <div class="dataTable_wrapper col-xs-12 div-scroll-y" id="content-ingresonota">
                                <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                                    <thead>
                                        <tr>
                                            <th class="text-center " style="vertical-align: middle" >Codigo</th>
                                            <th style="text-align: center;vertical-align: middle">Estudiante</th>
                                           <th style="text-align: center;vertical-align: middle"></th>
                                        </tr>
                                     
                                    </thead>
                                    <tbody>
                                        <input type = "hidden" value = "nada" id = "hdservicio" name = "hdservicio" />
                                {%for count ,alumnos in arr_datos %}
                                       <tr class="odd gradeX">
                                           <td width = "5%" style="text-align: center;">{{ alumnos.id_sg_usuario }}</td>
                                            <td width = "20%" style="text-align: center;">{{ alumnos.nombres }} <input type = "hidden" value = "{{ alumnos.nombres }}" name = "{{ alumnos.id_sg_usuario }}" /></td>
                                            <td width = "20%"> <span class="button-checkbox">
                                                                    <button type="button" class="btn" data-color="primary">Asistió</button>
                                                                    <input type="checkbox" class="hidden" value="{{ alumnos.id_sa_estudiante_carrera_materia }}" name="{{ alumnos.nombres }}" checked />
                                                                </span></td>
                                       </tr>
                                {% endfor %}
                                <input type = "hidden" value = "{{arr_datos|length}}" name = "hdcountalm" />
                                <input type = "hidden" value = "{{id_materia}}" name = "hdmateria" id="hdmateria" />
                                   </tbody>
                                </table>
                            </div>
                                    </div>
                            <!-- /.table-responsive -->
                    
                        </div>
                    
                    <!-- /.panel -->
                </div>
                <!-- /.col-lg-12 -->
            </div>
<div class="well">
     <h4>Nota :</h4>
     <p>Por su seguridad se recomienda verificar que las asistencias ingresadas sean las correctas</p>
     <div class="text-center ">
     <button type="button" class="btn btn-primary" id="btn-guardar" onclick="confirm('actualizaAsis')" >Actualizar</button>
     </div>
 </div>
                                   <div id="content-ingresonotas">
                                       </div>

</form>
           <script>                
   $(".ingresoNota").click(function(){
     var nombre =$(this).attr('id'); 
     var codigo =$(this).attr('name');
     
   //alert(nombre);
       mostrarAlumnos('ingresonotas','{{ path('docentes_ingreso_notas') }}',codigo,nombre);
     
   });
   </script>            
           <script>  
               
               $(function () {
    $('.button-checkbox').each(function () {

        // Settings
        var $widget = $(this),
            $button = $widget.find('button'),
            $checkbox = $widget.find('input:checkbox'),
            color = $button.data('color'),
            settings = {
                on: {
                    icon: 'glyphicon glyphicon-check'
                },
                off: {
                    icon: 'glyphicon glyphicon-unchecked'
                }
            };
                
        // Event Handlers
        $button.on('click', function () {
            $checkbox.prop('checked', !$checkbox.is(':checked'));
            $checkbox.triggerHandler('change');
            updateDisplay();
            
        });
        $checkbox.on('change', function () {
            updateDisplay();
        });

        // Actions
        function updateDisplay() {
            var isChecked = $checkbox.is(':checked');

            // Set the button's state
            $button.data('state', (isChecked) ? "on" : "off");

            // Set the button's icon
            $button.find('.state-icon')
                .removeClass()
                .addClass('state-icon ' + settings[$button.data('state')].icon);

            // Update the button's color
            if (isChecked) {
                $button
                    .removeClass('btn-default')
                    .addClass('btn-' + color + ' active');
            }
            else {
                $button
                    .removeClass('btn-' + color + ' active')
                    .addClass('btn-default');
            }
        }

        // Initialization
        function init() {

            updateDisplay();

            // Inject the icon if applicable
            if ($button.find('.state-icon').length === 0) {
                $button.prepend('<i class="state-icon ' + settings[$button.data('state')].icon + '"></i> ');
            }
        }
        init();
    });
});





var     new_checkbox_button = function(name,id){
            return "<li><span class='button-checkbox'><button type='button' class='btn btn-sm btn-primary active' data-color='primary'><i class='state-icon glyphicon glyphicon-check'></i>&nbsp;"+name+"</button><input type='checkbox' class='hidden' checked=''></span></li>";
        };

$("#add").click(function(){
       $("#selected_targets").append(new_checkbox_button("lallalala","1111"));
});

   </script>                                 
                                   


